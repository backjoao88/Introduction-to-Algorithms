/**
 *
 * 2.1-2
 * Consider the procedure SUM-ARRAY on the facing page. It computes the sum of
 * the n numbers in array A[1:N]. State a loop invariant for this procedure, and use
 * its initialization, maintenance, and termination properties to show that the SUM-ARRAY procedure returns the sum of the numbers
 * in A[1:N]
 *
 * R:
 * Loop invariant: The sum of the all the values in the subarray A[0:k] must be contained inside a variable called sum at each i-
 * teration k.
 *
 */