/**
 *
 * 12.2-5
 * Show that if a node in a binary search tree has two children, then its successor has
 * no left child and its predecessor has no right child.
 *
 * R: Because in this case, the successor of a number must be the min number of it's right side. Then if it's the lowest value, it has no left child.
 * The same goes for predecessor, but for the right child.
 *
 *
 */